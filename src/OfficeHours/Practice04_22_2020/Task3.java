package OfficeHours.Practice04_22_2020;

public class Task3 {}
   /* NOTE: MUST apply method overloading
	5. write an int[] return method that can sort an int array in desending order
	6. write a double[] return method that can sort a double array in desending order
	7. write a char[] return method that can sort a char array in desending order
    NOTE: MUST apply method overloading*/
/*   public static void main(String[] args) {
       int[] arr = {8, 16, 32, 64, 128};
       int[] result = decending(arr);
       System.out.println(Arrays.toString(arr));
       //===============================================
       double[] arr1 = {8, 16, 32, 64, 128};
       double[] result1 = decending(arr1);
       System.out.println(Arrays.toString(arr1));
       //============================================
       char[] arr2 = {'A', 'B', 'C', 'D', 'E'};
       char[] result2 = decending(arr2);
       System.out.println(Arrays.toString(arr2));
       //================================================
       public static int[] decendin ( int[] arr){
           Arrays.sort(arr);
           int[] decending = new int[arr.length];
           int j = arr.length - 1;
           for (int i = 0; i < decending.length; i++) {
               decending[i] = arr[j];
               j--;
           }
           return decending;
       }
       public static double[] decending1 ( double[] arr1){
           Arrays.sort(arr);
           double[] descending = new double[arr.length];
           int j = arr.length - 1;
           for (int i = 0; i < descending.length; i++) {
               descending[i] = arr[j];
               j--;
           }
           return descending;
       }
       public static char[] decending2 ( char[] arr){
           Arrays.sort(arr);
           char[] descending = new char[arr.length];
           int j = arr.length - 1;
           for (int i = 0; i < descending.length; i++) {
               descending[i] = arr[j];
               j--;
           }
           return descending;
       }
   }}
*/