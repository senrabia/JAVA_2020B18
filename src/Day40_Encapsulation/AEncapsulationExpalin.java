package Day40_Encapsulation;

public class AEncapsulationExpalin {
    /*Encapsulation: hiding the data
    1. hide the data nby giving private access modifier
    2.use the private data throught getter and setter

    GETTER(READ ONLY): public instance method =
    return-type must eith instance variaable data type
    -no parameter
   - returning the private instance variable

   SETTER():public instance method=
    -return-type is void
    -passes a parameter
    -parameter must match with instance variables dataType
    -initializes the argument to private instance variable
    ====================================================
    creating object:
    new ExistingConstructor

    task:- create a calass called credentials
         - privire instance variable
         username, password
         // private String userName;
          //private String password;
  static:
             static variables
             static methods
             static block
             static only inner calass. (nested) class


     */
}
