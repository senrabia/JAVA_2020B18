package Day30_ArrayList;

import java.sql.SQLOutput;
import java.util.ArrayList;

public class ArrayList_Reverse2 {
    public static void main(String[] args) {
        /* 2. write a program that can print the list of integers in reversed order
        ex:
        list=> {1,2,3,4,5}
        output: 5 4 3 2 1*/


        //============== ArrayList reversedList ===========

        ArrayList<Integer> list = new ArrayList<>();
           for(int i=1; i<6; i++){
                list.add(i);
            }
            System.out.println(list); //List
            ArrayList<Integer> reversedlist =new ArrayList<>();
            for(int i=list.size()-1; i>=0;  i--){
                int eachNum=list.get(i);  // assign unboximg
              reversedlist.add(eachNum);
            }
           System.out.println(reversedlist);

    /* My EXERCISE
        ArrayList<Integer> list1 = new ArrayList<>();
        for (int i = 0; i < 10; i++) {
            list.add(i);
        }

        ArrayList<Integer> reversedList = new ArrayList<>();

        for (int i = 1; i < 2; i++)

            for (int m = list.size() - 1; m >= 0; m--) {
                int eachNum = list.get(m);
                reversedList.add(eachNum);


                System.out.println(reversedList);
            }
        System.out.println(reversedList);*/
    }
        }









































































































































































































































































































































































