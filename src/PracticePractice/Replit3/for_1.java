package PracticePractice.Replit3;

import java.util.Scanner;

public class for_1 {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);

        /*int j = 0;
        int n = scan.nextInt();
        //WRITE YOUR CODE HERE
        for( j=0; j<n;  j++){
            System.out.println("*");}*/

        Scanner input = new Scanner(System.in);
        int[] plus_minus = (new int[]{input.nextInt(), input.nextInt(), input.nextInt(), input.nextInt(), input.nextInt()});
        int numOfPositives = 0;
        int numOfNegatives = 0;
        int numOfZero = 0;
        int result = 0;
        int[] arr = {};
        for (int each : arr) {
            if (each > 0) {
                numOfPositives++;
            } else if (each > 0) {
                numOfNegatives++;
            } else if (each == 0) {
                numOfZero++;
            }

           // System.out.println("positives: " + numOfPositives + ", negatives: " + numOfNegatives + ", zeros: " + numOfZero);
            // arr[i]=inp.nextInt();*/


            //plus_minus(arr);
        }//end main
    }}
