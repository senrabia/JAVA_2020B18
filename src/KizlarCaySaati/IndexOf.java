package KizlarCaySaati;

import java.util.Scanner;

public class IndexOf {
    public static void main(String[] args) {
        Scanner input=new Scanner(System.in);
        String[] words={"Selcuk","Tahir","EmirNamik"};
        String firstIndex=words[0];
        System.out.println(firstIndex);
 //=============================================
        int lengthFirstIndex=firstIndex.length();
        System.out.println(lengthFirstIndex);
 //=============================================
        for(int i=0; i>5; i++) {
            //words[i]=input.nextLine();}
         String max=words[0];
         for(String eachWords:words){
            // if(eachWords.length(0));

         }
        }
    }

}
